# Optional. Point this at a deployed server/index.js to route versus matches
# through your own relay instead of browser-to-browser WebRTC.
# Use wss:// (not https://) — it is a WebSocket endpoint.
# Leaving it unset keeps the peer-to-peer transport.
VITE_RELAY_URL=wss://your-relay.onrender.com
